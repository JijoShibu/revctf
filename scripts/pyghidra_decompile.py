# pyghidra_decompile.py — Ghidra 11.x+ headless post-script (PyGhidra runtime).
# Implemented in: M3.
# Emits a function list and decompiled pseudo-C for the analyzed program to stdout,
# which stage_ghidra.sh captures into the report fragment.
raise NotImplementedError("pyghidra_decompile: implemented in M3")
